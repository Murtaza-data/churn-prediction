# Backend (FastAPI) + frontend (Streamlit) share this file, like your other projects.
# ML lib versions pinned to EXACTLY match the Colab training env (confirmed 2026-07-13),
# so the saved XGBoost model unpickles cleanly on Render.
fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.9.2
joblib==1.4.2
xgboost==3.3.0
scikit-learn==1.6.1
pandas==2.2.2
numpy==2.0.2
requests==2.32.3
streamlit==1.40.0
